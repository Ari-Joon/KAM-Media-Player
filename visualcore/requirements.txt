librosa>=0.11.0
soundfile>=0.12
pydantic>=2.0

# Optional: lyric transcription.
#
# Adds word-level timings and per-section mood to the score, which the
# visualiser uses to choose choreography and colour. Pulls in CTranslate2 and
# downloads a ~140MB model on first use, so it is left commented out - without
# it every visualisation falls back to the audio features alone.
#
# faster-whisper>=1.0
